@echo off

echo set config for github

git config --local user.name "proger0014"
git config --local user.email "iakov56@yandex.ru"

git remote set-url origin https://github.com/Proger0014/multi-tasks.git

echo config is installed